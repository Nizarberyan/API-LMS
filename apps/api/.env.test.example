# Test Environment
NODE_ENV=test

# Database - Separate test database
MONGODB_URI=mongodb://localhost:27017/lms_test

# JWT Configuration
JWT_SECRET=test_jwt_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=test_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d

# SMTP (disabled in tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
SMTP_FROM=test@lms.com

# Server
PORT=3001
